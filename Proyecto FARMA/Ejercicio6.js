//Ejercicio: Dada una lista de números [1, 2, 3, 4, 5], usa .map() para crear 
//una nueva lista donde cada número esté elevado al cuadrado.

console.log([1, 2, 3, 4, 5].map(a => a*a));

//numeros.map(A => A 'LO QUE LE PASA A "A" ' )