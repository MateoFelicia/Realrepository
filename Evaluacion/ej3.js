const personas = [ { nombre: 'Juan', edad: 30 },
{ nombre: 'María',edad: 25 },
{ nombre: 'Pedro', edad: 35 } ];

console.log(personas.filter((p) => p.strings)       //Filtro solo los strings (no queremos numeros)
.map(Uppercase));                                //Map para aplicarle mayusculas a todas las variables
                                  //uppercase
