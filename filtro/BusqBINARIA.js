let lista = [2,3,4,5,7,8,9] //Si buscamos un elemento en un conjunto ordenado, dividimos el conjunto en dos partes (iguales)
let x = 7 // y conservamos aquella mitad donde podría encontrarse el elemento buscado. Volvemos a repetir este proceso
//  con la mitad seleccionada y continuamos hasta encontrar el valor buscado.

function BinarySearch(){
for (let i=math.floor(lista.lenght/2),j=lista.length; i < j, j > i; i++, j--) //i=principio de lista, j=final de lista
    j/2


}